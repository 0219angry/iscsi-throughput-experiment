#include <stdio.h>
#include <stdlib.h>
#include <poll.h>

#include "iscsi.h"
#include "scsi-lowlevel.h"



int main(int argc, char const *argv[]) {
  struct iscsi_context *iscsi;
  struct pollfd pfd;

  exit(EXIT_SUCCESS);
}